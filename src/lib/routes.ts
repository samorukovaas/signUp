export enum PrivateRoutes {
  main = '/',
  payments = '/payments',
  credit = '/credit',
  transfers = '/transfers',
  cards = '/cards',
  account = '/account',
}

export enum PublicRoutes {
  auth = '/auth',
  info = '/info',
}
